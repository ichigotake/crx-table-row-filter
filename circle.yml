dependencies:
  pre:
    - sudo apt-get update && sudo apt-get install jq

test:
  override:
    - ./ready-to-contribution.sh && export BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD) && export TAG=$([ "$BRANCH_NAME" == "master" ] && cat .next-version || echo "$BRANCH_NAME"; ) && ./make-release.sh

